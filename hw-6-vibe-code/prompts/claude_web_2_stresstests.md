# User
Сгенерируй скрипт для нагрузочного тестирования этого приложения, используя инструмент Locust (на Python) или k6.  Сценарий (Logical): 1. Пользователь обращается к главному эндпоинту. 2. Имитация нагрузки с постепенным увеличением до 100 пользователей (RPS).  Вывод (Explicit): - Предоставь полный код скрипта. - Напиши команду для запуска теста. - Объясни, как интерпретировать метрики: latency (задержка) и error rate (процент ошибок) в результатах этого скрипта.

# Model
Я создам для вас скрипт нагрузочного тестирования с использованием Locust, который более популярен в Python-экосистеме и проще в использовании.Теперь создам документацию по запуску и интерпретации результатов:Отлично! Я создал для вас полноценное решение для нагрузочного тестирования:

## 📊 Что включено:

### 1. **Locust скрипт** (`locustfile.py`)
- ✅ Реалистичная симуляция поведения пользователей
- ✅ Тестирование всех основных эндпоинтов (GET и POST)
- ✅ Кастомная load shape с постепенным увеличением до 100 пользователей
- ✅ Весовые коэффициенты для задач (более частые действия имеют больший вес)
- ✅ Логирование и обработка ошибок

### 2. **Подробное руководство**
- 📝 Пошаговая инструкция по установке и запуску
- 📊 Детальное объяснение метрик
- 🔍 Как интерпретировать результаты
- 💡 Рекомендации по оптимизации

## 🚀 Быстрый старт:

```bash
# 1. Установите Locust
pip install locust

# 2. Запустите приложение
docker-compose up -d
uv run python main.py

# 3. Запустите тест с Web UI
locust -f locustfile.py --host=http://localhost:8888

# 4. Откройте http://localhost:8089
```

## 🎯 Ключевые метрики:

**Latency (задержка):**
- **< 200ms** — отлично
- **200-500ms** — приемлемо  
- **> 500ms** — требует оптимизации

**Error Rate:**
- **< 0.1%** — отлично
- **< 1%** — приемлемо
- **> 5%** — критическая проблема

Скрипт автоматически генерирует HTML-отчёты с графиками и детальной статистикой!