# Руководство по нагрузочному тестированию

## Установка Locust

```bash
# Установка через pip
pip install locust

# Или через uv (если используется в проекте)
uv pip install locust
```

## Подготовка к тестированию

1. **Запустите приложение:**
```bash
# В первом терминале запустите Redis
docker-compose up -d

# Во втором терминале запустите приложение
cd python3-app
uv run python main.py
```

2. **Сохраните скрипт:**
```bash
# Сохраните скрипт как locustfile.py в директории python3-app
# или в любой удобной директории
```

## Команды для запуска тестирования

### Вариант 1: Web UI (рекомендуется для первого запуска)

```bash
locust -f locustfile.py --host=http://localhost:8888
```

Затем откройте браузер: **http://localhost:8089**

В Web UI:
- **Number of users**: 100 (максимальное количество)
- **Spawn rate**: 10 (скорость добавления пользователей в секунду)
- **Host**: http://localhost:8888 (уже указан)

Нажмите **Start swarming**

### Вариант 2: Headless режим (без UI, с автоматической генерацией отчётов)

```bash
# Полный тест с нашей кастомной load shape
locust -f locustfile.py \
  --host=http://localhost:8888 \
  --headless \
  --html=report.html \
  --csv=results
```

### Вариант 3: Быстрый тест (2 минуты)

```bash
# Без кастомной load shape - быстрый тест с фиксированной нагрузкой
locust -f locustfile.py \
  --host=http://localhost:8888 \
  --headless \
  --users=50 \
  --spawn-rate=5 \
  --run-time=2m \
  --html=quick_test.html
```

## Интерпретация метрик

### 1. Latency (Задержка)

**Что это:** Время отклика сервера на запрос (в миллисекундах).

#### Основные показатели:

| Метрика | Описание | Хорошие значения |
|---------|----------|------------------|
| **Average** | Среднее время отклика | < 200ms отлично, < 500ms приемлемо |
| **Median (50%)** | 50% запросов быстрее этого значения | < 100ms отлично |
| **95th percentile** | 95% запросов быстрее этого значения | < 500ms хорошо, < 1000ms приемлемо |
| **99th percentile** | 99% запросов быстрее этого значения | < 1000ms приемлемо |
| **Max** | Максимальное время отклика | Показывает худший случай |

#### Пример интерпретации:

```
Type     Name                    # reqs   Median   Average   95%ile   99%ile    Max
GET      /                        5000     45ms     52ms      98ms     145ms    320ms
POST     /hospital                1200     120ms    135ms     280ms    450ms    890ms
```

**Анализ:**
- ✅ **GET /** — отличная производительность (медиана 45ms)
- ⚠️ **POST /hospital** — приемлемо, но есть всплески до 890ms
- 📊 95% запросов к главной странице обрабатываются за < 100ms

### 2. Error Rate (Процент ошибок)

**Что это:** Процент неудачных запросов от общего числа.

#### Категории ошибок:

| HTTP код | Тип | Причина |
|----------|-----|---------|
| **400** | Client Error | Неверные данные, валидация не прошла |
| **500** | Server Error | Ошибка в приложении или Redis |
| **Connection Failed** | Network | Сервер не отвечает, таймаут |

#### Целевые значения:

- **< 0.1%** — отлично (практически без ошибок)
- **0.1% - 1%** — приемлемо (единичные ошибки)
- **1% - 5%** — проблемы (требуется оптимизация)
- **> 5%** — критично (система не справляется с нагрузкой)

#### Пример интерпретации:

```
Type     Name              # reqs   # fails   Fail%
GET      /                 10000    5         0.05%
POST     /patient          2500     125       5.00%
GET      /hospital         8000     0         0.00%
```

**Анализ:**
- ✅ **GET /** — отлично (0.05% ошибок)
- ✅ **GET /hospital** — идеально (0% ошибок)
- ❌ **POST /patient** — проблема! 5% ошибок требует исследования

### 3. RPS (Requests Per Second)

**Что это:** Количество запросов в секунду, которые обрабатывает система.

- **Current RPS**: текущая нагрузка
- **Target RPS**: 100 (в нашем тесте)
- **Max RPS**: максимальная достигнутая пропускная способность

**Хорошие показатели для Redis-приложения:**
- Small app: 100-500 RPS
- Medium app: 500-2000 RPS
- Large app: 2000+ RPS

### 4. Concurrent Users (Одновременные пользователи)

В нашем тесте:
- **Начало**: 0 пользователей
- **Постепенно**: до 100 пользователей за 5 минут
- **Пик**: 100 пользователей (5-8 минуты теста)
- **Спад**: обратно до 0 за 2 минуты

## Признаки проблем производительности

### 🔴 Критические проблемы:

1. **Latency растёт с увеличением пользователей**
   - Причина: Узкое место в коде или БД
   - Решение: Профилирование, оптимизация запросов

2. **Error rate > 5%**
   - Причина: Система не справляется
   - Решение: Масштабирование, оптимизация

3. **Timeout errors**
   - Причина: Сервер не успевает отвечать
   - Решение: Увеличить timeout или оптимизировать код

### ⚠️ Предупреждения:

1. **95th percentile в 3-5 раз больше медианы**
   - Проблема: Непостоянная производительность
   - Возможно: GC паузы, конкуренция за ресурсы

2. **Постепенный рост latency**
   - Проблема: Утечка памяти или накопление данных
   - Действие: Мониторинг ресурсов

## Полезные команды

### Просмотр результатов в реальном времени (Web UI):

```bash
# Статистика доступна по адресу
http://localhost:8089
```

### Генерация отчётов после теста:

```bash
# HTML отчёт (наглядный)
--html=report.html

# CSV файлы (для анализа)
--csv=results
# Создаст: results_stats.csv, results_failures.csv, results_stats_history.csv
```

### Анализ CSV файлов:

```bash
# Быстрый просмотр статистики
cat results_stats.csv | column -t -s,

# Фильтрация ошибок
cat results_failures.csv | grep "POST"
```

## Рекомендации по оптимизации

### Если latency высокая:

1. **Добавить индексирование** (Redis Sorted Sets для списков)
2. **Кэшировать часто запрашиваемые данные**
3. **Использовать пайплайны Redis** для batch операций
4. **Оптимизировать циклы** в handlers (например, в GET запросах)

### Если много ошибок:

1. **Проверить логи приложения**: `tail -f app.log`
2. **Мониторить Redis**: `redis-cli monitor`
3. **Увеличить connection pool** Redis
4. **Добавить retry логику** для транзиентных ошибок

### Если RPS низкий:

1. **Использовать async/await** (Tornado уже поддерживает)
2. **Увеличить worker processes** (запустить несколько инстансов)
3. **Настроить connection pooling**
4. **Рассмотреть load balancer** (nginx, HAProxy)

## Пример успешного теста

```
Benchmark Results:
==================
Total Requests: 45,000
Success Rate: 99.95%
Average Latency: 85ms
95th Percentile: 230ms
RPS: 75 (constant load)
Duration: 10 minutes
```

Это означает:
- ✅ Система стабильна под нагрузкой
- ✅ Почти все запросы успешны
- ✅ Быстрое время отклика
- ✅ Можно масштабировать дальше

## Дополнительные ресурсы

- [Документация Locust](https://docs.locust.io/)
- [Best Practices for Load Testing](https://docs.locust.io/en/stable/running-distributed.html)
- [Redis Performance Tips](https://redis.io/docs/management/optimization/)
